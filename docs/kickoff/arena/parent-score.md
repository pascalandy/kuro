# Parent assessment before cross-judge verdict

All nine candidate files read end-to-end after completion. No dropout in SQ round. The two interrupted local-first candidates are a superseded attempt, not failed SQ candidates.

| Criterion | A | B | C |
| --- | ---: | ---: | ---: |
| Audio responsibilities and clocks | 2 | 2 | 2 |
| Fidelity to the new need | 2 | 2 | 2 |
| Protocol and hardware feasibility | 2 | 1 | 2 |
| Falsifiable synthetic proof | 2 | 2 | 2 |
| Proportionate architecture and language | 2 | 1 | 1 |
| Honest SQ conclusion and next test | 2 | 2 | 2 |
| Total | 12 | 10 | 11 |

Provisional base A. It keeps all library/queue/backup durability with one service and treats MPD as disposable execution state. This is a smaller public and persistence contract than C's cross-machine backup barrier or B's newly specified PCM protocol. All candidates separate packet timing, simulated consumption, and physical clocks appropriately.

Graft C's complete verified files before admission, bounded current/next reservation, content-version identity separate from catalogue reference, immutable published objects, atomic publication, and exact cache failure semantics. Do not graft renderer-owned durable queue or distributed snapshot activation. Verified staging is the first transport policy to test for the confirmed SQ priority and Aurender local-file reference. Keep progressive HTTP on the same engine as a comparison, not a claim of inferior sound.

Graft B's explicit canonical PCM description, negative block/file integrity controls and reproducible timing traces into the lab evidence. Do not graft custom PCM credits/framing, server decoding or GStreamer pipeline before an actual need defeats the existing renderer path.

A's Go prototype is attractive because the bounded service can use the standard library for HTTP/ranges/hash/JSON and process control. C's Rust choice provides stronger semantic type encoding and memory accounting, but no engine FFI benefit in the proposed file/control service. Choose a language for the retained experiment and a provisional service direction, not a final SQ or full-stack conclusion. Judge feedback and implementation friction may revise this choice.

All proposals need a clear separation between the large proposed product API and the tiny implemented transport experiment. A's select/prepare/start sequence must be one ordinary Play gesture in the eventual UI, with preparation internal and visible progress. Complete-staging delay and two caches are real costs to measure; unsupported format rejection remains a hardware experiment, not a guarantee inferred from MPD configuration.
